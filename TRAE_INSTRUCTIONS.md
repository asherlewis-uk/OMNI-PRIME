# OMNI-PRIME Development Phase: Trae Intelligence Layer**Objective:** Enhance the OMNI-PRIME agentic system with Long-Term Memory, Multimodal Processing, and MCP Tooling

---### 🛡️ STRICT BOUNDARIES (DO NOT IMPLEMENT)**Do not attempt the following tasks** as they require deep contextual system monitoring and real-time process debugging (reserved for Windsurf):1. **Docker/E2B Sandboxing:** Any logic involving `toolWorker.ts` execution in isolated containers.2. **Swarm Orchestration Logic:** The hand-off mechanics between specialized agents.3. **Global Auth/RBAC:** Implementing NextAuth or system-wide permission guards.

---### 🚀 ACTIVE TASKS (FOR IMPLEMENTATION)#### 1. Vector Memory & RAG Integration**Goal:** Extend the current SQLite storage to support semantic retrieval.- **Reference Files:** `src/lib/db/schema.ts`, `src/lib/db/client.ts`, `src/lib/ai/contextInjector.ts`.- **Action:** Propose a schema update or a separate vector-service client (e.g., Pinecone/pgvector) to store and retrieve agent "recollections" based on embedding similarity.#### 2. Multimodal Gateway Expansion**Goal:** Allow the system to handle images and audio within the message pipeline.- **Reference Files:** `src/lib/ai/unifiedGateway.ts`, `src/types/chat.ts`.- **Action:** Update the `UnifiedGateway` to process `base64` image data or audio buffers, ensuring compatibility with Gemini Vision/OpenAI models.#### 3. Standard MCP Tool Implementations**Goal:** Build out the actual "tools" that the system can use via the Model Context Protocol.- **Reference Files:** `src/lib/mcp/MCPServerManager.ts`, `src/lib/mcp/toolRegistry.ts`.- **Action:** Create a standard set of MCP servers for:    - **Local File Search:** Recursive search through the workspace.    - **Web Search:** Integration with Tavily/Serper.    - **System Metrics:** Basic CPU/Memory reporting tools.#### 4. Agent Trace UI (Observability)**Goal:** Create a dashboard component to visualize agent "thinking" processes.- **Reference Files:** `src/components/layout/DesktopShell.tsx`, `src/lib/stores/chatStore.ts`, `src/app/dashboard/page.tsx`.- **Action:** Design a React/Tailwind component that parses `<thinking>` tags from the stream and displays them in a "Process Log" sidebar, distinct from the main chat.

---### 📂 SYSTEM CONTEXT FOR LLM- **Frontend:** Next.js 14+ (App Router), Tailwind CSS, Radix UI.- **Backend/Queue:** BullMQ (Redis) for background jobs.- **Database:** Drizzle ORM + SQLite (local).- **Architecture:** "Deep Agent" Swarm structure (refer to `system-architecture.md` and `AGENTS.md`).
