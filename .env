# ═══════════════════════════════════════════════════════════════════════════════
# OMNI-PRIME — Environment Configuration
# Sovereign Pocket Enterprise — Local-First by Default
# ═══════════════════════════════════════════════════════════════════════════════

# ─── Runtime ──────────────────────────────────────────────────────────────────
NODE_ENV="development"

# ─── Database (SQLite via better-sqlite3 / Drizzle ORM) ──────────────────────
DATABASE_URL="file:./data/omni.db"

# ─── AI Gateway (Local-First Sovereignty) ─────────────────────────────────────
# Host-level Ollama instance accessible via Docker bridge
OLLAMA_URL="http://host.docker.internal:11434"

# ─── Redis (BullMQ job queue backend) ─────────────────────────────────────────
# "redis" is the Docker Compose service name on ai-stack-net
REDIS_URL="redis://redis:6379"

# ─── ChromaDB (Local vector storage) ─────────────────────────────────────────
CHROMA_URL="http://chroma:8000"

# ─── Cloud LLM Keys (Optional BYOK — Leave empty for offline operation) ──────
OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""